Games live in the `games` directory. Each game should export a `name: string`, `category: string | string[]` and `description` (jsx).

File names don't matter other than determining the order in which games are listed.